# API Configuration
# Only needed if connecting to backend API
REACT_APP_API_URL=http://localhost:8000/api

# DevTools Configuration
# Set to false to disable DevTools in production
REACT_APP_ENABLE_DEVTOOLS=true

# Optional: Custom port for development server
# PORT=3000

# Optional: Disable automatic browser opening
# BROWSER=none

# Optional: Skip preflight check
# SKIP_PREFLIGHT_CHECK=true
